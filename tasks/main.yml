---
- name: Install themes
  ansible.builtin.git:
    repo: "{{ item.repo }}"
    dest: "{{ item.install_dir }}"
    version: "{{ item.version }}"
  with_items:
    - "{{ redmine_themes }}"
  when: not ansible.builtin.stat:
      path: "{{ item.install_dir }}"
      isdir: yes
